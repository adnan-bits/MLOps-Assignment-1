apiVersion: v1
kind: Service
metadata:
  name: heart-disease-api-nodeport
  labels:
    app: heart-disease-api
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8000
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: heart-disease-api
  sessionAffinity: None

