apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: heart-disease-models-pvc
  labels:
    app: heart-disease-api
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard  # Change based on your cluster

